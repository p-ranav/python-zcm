ZeroMQ Component Model in Python
================================

A lightweight component model using ZeroMQ and Google Protobuf

ZCM Dependencies
----------------

* Install pyzmq

```bash
$ sudo pip install pyzmq
```